package com.TPI.service.menuService;

import java.util.Scanner;

public interface MenuService {
    //Evento evento = new Evento();
    //int opcion;
    //Scanner scanner = new Scanner(System.in);
    void mostrarMenu(Scanner scanner);
}
